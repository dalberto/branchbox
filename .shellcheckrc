# ShellCheck configuration for BranchBox

# Disable specific warnings that are intentional in our scripts
disable=SC1091  # Not following sourced files
disable=SC2181  # Check exit code directly (we use $? for clarity in tests)
disable=SC2155  # Declare and assign separately (we prefer compact syntax)
disable=SC2034  # Unused variables (some are for documentation/future use)
disable=SC2164  # Use 'cd ... || exit' (handled by set -e in tests)
disable=SC2046  # Quote to prevent word splitting (intentional in some cases)
disable=SC2154  # Referenced but not assigned (variables from environment)